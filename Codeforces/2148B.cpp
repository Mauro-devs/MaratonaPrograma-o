#include <bits/stdc++.h>

using namespace std;


int main(){

    int quant_rep;
    
    cin >> quant_rep;

    while(quant_rep--){
        int n, m, x, y = 0;

        cin >> n >> m >> x >> y;

        for(int i = 0; i<n; i++){
            int a;
            cin >> a;
        }

        for (int i = 0; i < m; i++){
            int b;
            cin >> b;
        }

        cout << n + m << "\n";
    }
}